{
  "name": "theographic-api",
  "version": "1.0.0",
  "description": "GraphQL API for Theographic Bible Metadata using JSON data",
  "main": "index.js",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build step required'",
    "test": "node --test tests/graphql.test.js",
    "test:verbose": "node --test --test-reporter=spec tests/graphql.test.js"
  },
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "graphql": "^16.6.0",
    "graphql-tag": "^2.12.6"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}
